{
  "name": "degen-score-backend",
  "version": "1.0.0",
  "description": "Backend API for DeGen Score - Crypto reputation analyzer on Base",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "keywords": [
    "crypto",
    "web3",
    "defi",
    "nft",
    "ethereum",
    "base",
    "blockchain",
    "api"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0"
  }
}
```

---

## 🎯 Comment corriger sur GitHub

### **Étape 1 : Aller sur le fichier**
1. Allez sur : https://github.com/Makabeez/Degen-score-app/blob/main/backend/package.json
2. Cliquez sur **✏️ Edit** (en haut à droite)

### **Étape 2 : Tout supprimer**
1. **Sélectionnez TOUT** (Ctrl+A ou Cmd+A)
2. **Appuyez sur Suppr** ou **Delete**

### **Étape 3 : Copier le bon fichier**
1. Copiez le JSON ci-dessus (du premier `{` au dernier `}`)
2. **Collez** dans l'éditeur GitHub
3. **Vérifiez** qu'il n'y a :
   - ❌ Pas de backticks (```)
   - ❌ Pas de texte "## 🚀"
   - ❌ Pas de "---"
   - ✅ Juste du JSON pur

### **Étape 4 : Sauvegarder**
1. En bas : Commit message → `Fix package.json - remove markdown formatting`
2. Cliquez sur **"Commit changes"**

---

## 🔍 Vérification visuelle

Votre `package.json` doit ressembler à ça (sans rien d'autre) :
```
{
  "name": "degen-score-backend",
  ...
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0"
  }
}
```

**Il NE doit PAS ressembler à ça :**
```
{
  ...
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0"
  }
}
```

---

## 🚨 Erreurs communes à éviter

| ❌ MAUVAIS | ✅ BON |
|------------|--------|
| Contient ``` | Pas de backticks |
| Contient du markdown | Uniquement JSON |
| Contient "## 🚀" | Aucun titre |
| Deux fois `dependencies` | Une seule fois |

---

## 🚀 Après la correction

Une fois le fichier corrigé sur GitHub :

1. **Retournez sur Render**
2. Cliquez **"Manual Deploy"** → **"Clear build cache & deploy"**
3. Attendez les logs

Vous devriez maintenant voir :
```
==> Running build command 'npm install'...
npm install
added 58 packages in 3s

==> Build completed successfully ✅

==> Starting service with: npm start
🚀 DeGen Score Backend running on port 10000
